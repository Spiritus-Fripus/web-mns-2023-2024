// Exercice 9:
// Créez un tableau de chaînes de caractères. Triez-les par ordre alphabétique.

const list = ["y", "z", "a", "s", "u", "b", "d", "q"];

function sortString(array) {
    list.sort();
    console.log(array);
}

sortString(list);