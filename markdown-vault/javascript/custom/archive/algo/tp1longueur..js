// Ecrire la algo "lenght(word)" qui calcule la taille d'un mot. si word n'est pas une chaine de caractère alors retourner -1
// - avec un for
// - avec un while

const motATester = "nathan";

const array = ["zdada", "baam", "boom"];

function lengthWithFor(word) {
}

function lengthWithWhile(word) {
	let longueurDuMot;

	while (word[longueurDuMot]) {
		longueurDuMot++;
	}
	return longueurDuMot;
}
